import React from 'react';
import TodosApp from './components/todos/TodosApp';

const App = (props) => {
    return (
        <div>
            <TodosApp />
        </div>
    );
};

export default App;
